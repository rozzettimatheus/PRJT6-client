import React from 'react';

// import { Container } from './styles';

const Header: React.FC = () => {
  return (
    <header>
      <nav>
        <h1>Header</h1>
      </nav>
    </header>
  );
};

export default Header;
